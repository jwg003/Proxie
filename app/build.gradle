apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'proxie_app'
            keyPassword 'pizzaforest'
            storeFile file('/Users/John/Dropbox/JuniorYearSpring/Android/Proxie/Proxie.keystore.jks')
            storePassword 'pizzaforest'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "com.example.john.proxie"
        minSdkVersion 19
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'com.google.android.gms:play-services-maps:6.5.87'
}
